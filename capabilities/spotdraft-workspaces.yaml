naftiko: 1.0.0-alpha2
info:
  label: SpotDraft Public API — Workspaces
  description: SpotDraft Workspaces capability. 1 operations across versions v1/v2/v2.1.
  tags:
  - SpotDraft
  - CLM
  - Contracts
  - Workspaces
  created: '2026-05-25'
  modified: '2026-05-25'
binds:
- namespace: env
  keys:
    SPOTDRAFT_CLIENT_ID: SPOTDRAFT_CLIENT_ID
    SPOTDRAFT_CLIENT_SECRET: SPOTDRAFT_CLIENT_SECRET
capability:
  consumes:
  - type: http
    namespace: workspaces
    baseUri: https://api.us.spotdraft.com
    description: SpotDraft Workspaces endpoints.
    headers: {}
    resources:
    - name: v2-1-public-workspace-feature-flags
      path: /api/v2.1/public/workspace/feature-flags/
      operations:
      - name: v2-1-public-workspace-feature-flags-create
        method: POST
        description: Check workspace feature flags
        outputRawFormat: json
        inputParameters:
        - name: body
          in: body
          type: object
          required: true
